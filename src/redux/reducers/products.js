import data from '../../../products.json';
const initialDataState = data;
const Products = (state = initialDataState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default Products;
